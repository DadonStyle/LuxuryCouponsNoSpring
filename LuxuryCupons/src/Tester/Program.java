package Tester;

/**
 * Program class
 *  The main program with main method that exacutes the main method
 */
public class Program {
    /**
     * The main method  exacutes the "test all" method in "test" class
     * @param args contains the supplied command-line arguments as an array of String objects.
     */
    public static void main(String[] args) {
        Test.testAll();
    }
}
